import {ImageStyle, TextStyle, ViewStyle} from "react-native"

export interface I_PlayerFooterProps {}

export interface I_PlayerFooterStyles {
    container: ViewStyle;
    thumbnail: ImageStyle;
    textContainer: ViewStyle;
    topText: TextStyle;
    bottomText: TextStyle;
    icon: ViewStyle;
}